# Test runner
selenium==4.18.1
pytest==8.1.1
pytest-html==4.1.1

# Code quality — pre-commit installs these automatically,
# but pinning here keeps CI versions in sync
pre-commit==3.7.1
flake8==7.0.0
